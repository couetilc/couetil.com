#!/usr/bin/env bash

docker build -t connor.couetil.com:latest \
	-f "$connor/Dockerfile" \
	"$connor"

docker build -t connor.couetil.com:nginx \
	-f "$connor/Dockerfile" \
	-f "$connor/Dockerfile.nginx" \
	"$connor"
